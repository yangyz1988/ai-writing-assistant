# AI Writing Assistant - P0 + P1 功能更新

## 更新时间: 2026-07-06

---

## P0 核心功能 ✅

### 1. 结果预览确认机制

- AI 处理完成后弹出预览窗口
- 左侧原文、右侧改写结果对比
- 变化统计：增加字数、删除字数、相似度百分比
- 点击"确认替换"后才执行替换
- 支持 Esc 键或点击遮罩关闭

### 2. 快捷键支持

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Shift+W` (Windows) | 唤起浮动菜单 |
| `Command+Shift+W` (Mac) | 唤起浮动菜单 |
| 数字键 `1-9` | 快速选择写作模式（含自定义模式） |
| `Esc` | 关闭菜单/预览窗口 |

### 3. Shadow DOM 样式隔离

- 浮动菜单和 Toast 在 Shadow DOM 中
- 网页 CSS 无法污染扩展样式
- 扩展样式不会泄露到网页

---

## P1 体验优化 ✅

### 4. 历史记录

- 保存最近 50 条改写记录
- 每条记录：原文、改写结果、模式、时间戳、token 消耗
- Popup 新增"历史"标签页
- 功能：列表展示、详情查看、复制结果、删除记录、清空全部

### 5. 自定义写作模式

- 用户可添加自己的写作模式
- 每个模式：名称、图标、描述、自定义 Prompt
- Popup 新增"自定义"标签页
- 功能：添加、编辑、删除自定义模式
- 自定义模式与内置模式一起显示在浮动菜单和右键菜单
- 存储在 chrome.storage.sync

### 6. Token 费用统计

- 每次 API 调用自动记录 token 消耗
- 支持 DeepSeek、通义千问、智谱GLM、OpenAI、Anthropic
- Popup 新增"统计"标签页
- 显示：今日/本月/总消耗 tokens
- 根据不同服务商价格估算费用
- 支持重置统计数据

---

## 技术实现

### 修改文件清单

| 文件 | 改动 |
|------|------|
| `src/content/index.ts` | Shadow DOM、预览窗口、快捷键、历史保存、自定义模式显示 |
| `src/background/index.ts` | 快捷键命令、历史记录管理、Token 统计、自定义模式支持 |
| `src/popup/App.tsx` | 四个标签页：配置、历史、自定义、统计 |
| `src/popup/styles.css` | 所有新界面样式 |
| `src/shared/types.ts` | 新类型：HistoryRecord、TokenStats、TokenRecord、CustomWritingMode |
| `src/shared/constants.ts` | 新常量：TOKEN_PRICES、AVAILABLE_ICONS、价格计算函数 |
| `manifest.json` | 快捷键命令声明 |

### Popup 界面结构

```
[配置] [历史] [自定义] [统计]
  │      │       │       │
  │      │       │       │
 API    记录    添加    今日消耗
配置    列表    模式    本月消耗
       详情    编辑    总消耗
       复制    删除    调用记录
       清空           重置
```

---

## 使用方法

### Edge 浏览器加载

```
路径: D:/ai专用/codex zy/chajian/dist
步骤: edge://extensions/ → 开发人员模式 → 加载解压缩的扩展
```

### 配置 API

1. 点击扩展图标
2. 选择服务商（推荐 DeepSeek）
3. 输入 API Key
4. 选择模型
5. 点击"测试连接"
6. 保存设置

### 使用方式

**方式一：鼠标选择**
1. 选中文本（至少3字符）
2. 等待浮动菜单
3. 点击模式按钮
4. 预览后确认替换

**方式二：快捷键**
1. 选中文本
2. `Ctrl+Shift+W` 唤起菜单
3. 数字键 `1-9` 选择模式
4. 预览后确认替换

### 自定义模式

1. 点击扩展图标 → "自定义"标签
2. 点击"添加模式"
3. 填写名称、选择图标、描述、Prompt
4. 保存后自动出现在浮动菜单

### 查看历史

1. 点击扩展图标 → "历史"标签
2. 查看所有改写记录
3. 点击详情查看原文/改写对比
4. 复制结果或删除记录

### 查看费用

1. 点击扩展图标 → "统计"标签
2. 查看 tokens 消耗和费用估算
3. 查看每次调用详情
4. 可重置统计数据

---

## 价格参考

| 服务商 | 输入价格 | 输出价格 |
|--------|----------|----------|
| DeepSeek | ¥1/百万tokens | ¥2/百万tokens |
| 通义千问 turbo | ¥0.008/千tokens | ¥0.02/千tokens |
| 智谱GLM-4 | ¥0.1/千tokens | ¥0.1/千tokens |
| GPT-4 | $0.03/千tokens | $0.06/千tokens |
| Claude Sonnet | $0.003/千tokens | $0.015/千tokens |

---

## 下一步 P2 建议

1. **暗色模式** - 跟随系统主题
2. **批量处理** - 处理多个选区
3. **多语言界面** - 英文/中文切换
4. **语音输入** - 语音转文字
5. **导出配置** - 导出/导入设置和自定义模式

---

## 构建文件

```
dist/
├── background.js   (9.35 KiB)
├── content.js      (19.8 KiB)
├── popup.js        (148 KiB)
├── popup.css       (7.81 KiB)
├── popup.html
├── manifest.json
└── icons/
```