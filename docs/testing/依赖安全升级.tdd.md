# 依赖安全升级：验证证据

## RED 基线

- `npm audit`：7 个漏洞，包括 4 moderate、2 high、1 critical。
- 主要来源：Vitest 2.x → Vite/esbuild；copy-webpack-plugin 12.x → serialize-javascript。

## 修复

- `vitest`：升级至 `4.1.10`。
- `copy-webpack-plugin`：升级至 `14.0.0`。
- `package-lock.json` 已由 npm 重建相关依赖树。

## GREEN 证据

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 依赖审计 | `npm audit` | 0 vulnerabilities |
| 自动化测试 | `npm test` | 3 个文件、18 个测试通过 |
| 类型检查 | `npm run typecheck` | PASS |
| 代码规范 | `npm run lint` | PASS |
| 生产构建 | `npm run build` | Webpack 编译成功 |

## 已知范围

- 本次升级只涉及开发依赖，没有自动执行 `npm audit fix --force`。
- 尚未配置 Dependabot 或 CI 定期审计。
